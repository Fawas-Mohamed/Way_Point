# packages/types — lands in Phase 3

Shared Zod schemas / DTOs used by both apps/api (server-side validation)
and apps/web (client-side form validation), so both sides validate against
the exact same shape.
