# See the root .env.example for the full list — this app reads the same
# variables. When running outside Docker Compose, copy the root .env into
# apps/api/.env or export the variables in your shell.
