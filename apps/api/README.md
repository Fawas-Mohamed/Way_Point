# apps/api — lands in Phase 3

Node.js + Express REST API. Clean-architecture layers (routes → controllers
→ services → repositories), JWT auth + refresh tokens, RBAC middleware.
See ARCHITECTURE.md §2–3 at the repo root.
